# Tracky Frontend

## Setup

1. Install packages:

```bash
npm install
```

2. Start the Vite dev server:

```bash
npm run dev
```

The app expects the Tracky backend to run on `http://localhost:5000` by default.
